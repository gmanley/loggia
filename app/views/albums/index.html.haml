/ %ol#albums.thumbnails
/   = render partial: 'albums/album', collection: @albums

/ - if can?(:create, Album)
/   = link_to('New Album', new_album_path, class: 'btn btn-primary')

:coffeescript
  $ ->
    new App.Routers.AlbumsRouter(albums: #{render(partial: 'index', formats: :json, object: @albums)})
    App.handleHistory()
