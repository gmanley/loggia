#recent-albums
  = link_to(root_url(format: :atom), class: 'rss-link') do
    %i.icon-rss.icon-2x

  %header
    %h3 Recent

  .grid-container{style: 'display:none'}
    = render @recent_albums

#albums.grid-container{style: 'display:none'}
  = render @albums

- if can?(:create, Album)
  = link_to('New Album', new_album_path, class: 'btn btn-primary')
