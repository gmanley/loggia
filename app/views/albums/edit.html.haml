= form_for([@category, @album]) do |f|
  %fieldset
    %legend Editing Album
    = render partial: 'containers/form', locals: {f: f}
    .form-actions
      = f.submit 'Save', class: 'btn btn-primary'

= link_to 'Show', [@category, @album]
