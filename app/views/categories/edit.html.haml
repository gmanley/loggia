= form_for(@category) do |f|
  %fieldset
    %legend Editing Category
    = render partial: 'containers/form', locals: {f: f}
    .form-actions
      = f.submit 'Update', class: 'btn btn-primary'

= link_to 'Show', @category
