%h2.page-header Home

%ul.thumbnails
  = render partial: 'containers/container', collection: @categories

= link_to_if(can?(:create, Category), 'New Category', new_category_path, class: 'btn btn-primary')